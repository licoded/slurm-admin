# Slurm Lifecycle Monitor Configuration
# Copy this file to .env.local and fill in your values

# Webhook URL for job notifications
# Supported formats: Feishu, Slack, Discord, or any custom webhook
SLM_WEBHOOK="https://your-webhook-url.com/webhook/path"

# Database Configuration (MySQL 5.7+)
# These are the defaults - override as needed for your environment
SLM_DB_HOST="your_mysql_host"
SLM_DB_PORT="3306"
SLM_DB_USER="slurm_admin_rw"
SLM_DB_PASSWORD="your_secure_password"
SLM_DB_NAME="slurm_admin"

# Optional: Default Slurm partition
# SLM_PARTITION="gpu"

# Optional: Default job time limit
# SLM_TIME="24:00:00"

# Optional: Enable debug logging
# SLM_DEBUG="true"
